package com.kw.common.math;

public class MyVector3{
//    public static Vector3 Lerp(Vector3 a, Vector3 b, float t)
//    {
//        t = Mathf.Clamp01(t);
//        return new Vector3(a.x + (b.x - a.x) * t, a.y + (b.y - a.y) * t, a.z + (b.z - a.z) * t);
//    }
}
