# LibGdxTool Desktop 案例运行说明

> 适用于：`dm-kangwang/LibgdxTool` 的 `libgdx1.13.1` 分支
>
> 目标：告诉你每个 Desktop 案例怎么启动、需要注意什么、依赖什么资源、适合什么时候看。

---

## 1. 总体运行方式

### 1.1 通用启动入口
大多数案例都继承自：

- `desktop/src/com/libGdx/test/base/LibGdxTestMain.java`

标准写法一般是：

```java
public class XxxApp extends LibGdxTestMain {
    public static void main(String[] args) {
        new XxxApp().start();
    }

    @Override
    public void useShow(Stage stage) {
        super.useShow(stage);
        // add actors here
    }
}
```

### 1.2 运行前检查
运行 Desktop 案例前，先确认：

1. 已切到 `libgdx1.13.1` 分支
2. Gradle / Java 环境可用
3. 资源文件已同步到对应目录
4. `assets`、`shader`、`cocos`、`spine` 等目录没有缺失
5. 如果是 3D / Bullet / Spine / Shader 案例，确认本机 OpenGL 和依赖库可运行

### 1.3 推荐启动顺序
如果只是想快速验证仓库是否正常：

1. `base`
2. `sprite`
3. `table`
4. `camera`
5. `file`
6. `json`
7. `thread`
8. `shader`
9. `sc`

---

## 2. 全量案例运行说明索引

> 说明：每一项都尽量写成“怎么跑 / 依赖什么 / 适合看什么”。
> 对于结构相似的模块，说明会合并写，避免重复。

### 2.1 `base`
- **启动方式**：直接运行继承 `LibGdxTestMain` 的入口类
- **依赖**：Scene2D 基础框架
- **注意**：这是所有案例的统一模板
- **适合看什么**：窗口创建、Stage 生命周期、基础 UI 接入

### 2.2 `action`
- **启动方式**：运行 `desktop/src/com/libGdx/test/action/NumActionTest.java`
- **依赖**：Scene2D Action
- **注意**：重点看自定义 Action 的 `act()` / `getValue()` 更新逻辑
- **适合看什么**：数值动画、缓动、进度变化

### 2.3 `alpha`
- **启动方式**：运行 `AlphaTest.java` 或 `AlphaTestApp.java`
- **依赖**：Scene2D、透明度属性
- **注意**：如果视觉不明显，可调大透明度变化幅度
- **适合看什么**：淡入淡出、控件透明继承

### 2.4 `anr`
- **启动方式**：运行对应 ANR 监控入口类
- **依赖**：线程监控逻辑
- **注意**：卡顿判断通常需要在主线程制造延迟才明显
- **适合看什么**：主线程卡顿检测、watchdog 思路

### 2.5 `asset`
- **启动方式**：运行资源加载测试类
- **依赖**：AssetManager 与资源文件
- **注意**：确保资源路径正确，否则会报找不到纹理/音频
- **适合看什么**：异步加载、资源统一管理、释放流程

### 2.6 `ball`
- **启动方式**：运行球体/运动测试入口
- **依赖**：基础数学与渲染
- **注意**：如果是物理型演示，确认重力、弹跳参数
- **适合看什么**：位移、速度、加速度、碰撞反弹

### 2.7 `beser`
- **启动方式**：运行 `B.java` / `BU.java` / `BUL.java` / `BUL1.java`
- **依赖**：数学计算与绘制
- **注意**：注意曲线采样点数量，太少会锯齿，太多会影响性能
- **适合看什么**：贝塞尔曲线、轨迹绘制、路径插值

### 2.8 `bianyuan`
- **启动方式**：运行描边效果入口类
- **依赖**：图像处理 / 可能使用 Shader
- **注意**：描边厚度与采样半径会影响最终效果
- **适合看什么**：文字描边、图像轮廓增强

### 2.9 `bloom`
- **启动方式**：运行 `BloomDemo.java`
- **依赖**：FBO、Shader、后处理流程
- **注意**：要确认 OpenGL 环境支持 FrameBuffer
- **适合看什么**：高亮提取、模糊、辉光合成

### 2.10 `bullet`
- **启动方式**：运行 `App.java`
- **依赖**：Bullet 3D 物理库
- **注意**：如果报库加载错误，检查 Bullet native 依赖
- **适合看什么**：刚体、重力、碰撞、物理世界更新

### 2.11 `camera`
- **启动方式**：运行 `App.java` / `Closeup.java` / `DemoCamera.java` / `OrthographicProjection.java`
- **依赖**：Camera / Viewport
- **注意**：常见问题是坐标转换和缩放范围
- **适合看什么**：跟随镜头、缩放、正交投影、局部放大

### 2.12 `cir`
- **启动方式**：运行圆形绘制入口
- **依赖**：ShapeRenderer
- **注意**：圆形精度与分段数有关
- **适合看什么**：圆、弧、扇形基础绘制

### 2.13 `cirprogres`
- **启动方式**：运行圆形进度条入口
- **依赖**：圆形绘制 / 裁剪 / 可能 Shader
- **注意**：看角度映射是否正确
- **适合看什么**：环形进度、CD 组件

### 2.14 `click`
- **启动方式**：运行点击测试入口
- **依赖**：InputListener / ClickListener
- **注意**：确认输入事件绑定到正确 Actor
- **适合看什么**：点击、按下、抬起、回调

### 2.15 `clip`
- **启动方式**：运行裁剪测试入口
- **依赖**：ScissorStack / 裁剪区域
- **注意**：裁剪区域通常要先计算坐标，再 push/pop
- **适合看什么**：局部显示、滚动区域、遮罩裁切

### 2.16 `cocos`
- **启动方式**：运行 Cocos 资源加载入口
- **依赖**：Cocos 导出的 JSON 资源
- **注意**：资源路径要和 JSON 中的路径对应
- **适合看什么**：Cocos 节点树导入、兼容加载

### 2.17 `color`
- **启动方式**：运行 `ImageColor.java` / `ColorConvert.java`
- **依赖**：Color
- **注意**：Hex 字符串格式要正确
- **适合看什么**：HSV、RGB、Hex 转换

### 2.18 `colorcircle`
- **启动方式**：运行色环选择器入口
- **依赖**：Pixmap / 触摸交互
- **注意**：色环中心和半径要匹配
- **适合看什么**：颜色选择器、HSV 交互

### 2.19 `common`
- **启动方式**：通常不是单独运行，而是被其他模块引用
- **依赖**：取决于具体公共组件
- **注意**：先看有哪些工具类被复用
- **适合看什么**：公共 UI / 公共逻辑封装

### 2.20 `connectdot`
- **启动方式**：运行 `ConnectDotApp.java` 或 `ConnectDotApp2.java`
- **依赖**：Scene2D、连线逻辑
- **注意**：触摸点判定和连线状态机是重点
- **适合看什么**：点连接、路径记录、玩法逻辑

### 2.21 `csv`
- **启动方式**：运行 CSV 解析测试入口
- **依赖**：CSV 文件
- **注意**：分隔符、换行、引号处理要注意
- **适合看什么**：表格数据导入

### 2.22 `cut`
- **启动方式**：运行图片裁切入口
- **依赖**：Pixmap / TextureRegion
- **注意**：裁切坐标要和图片实际尺寸一致
- **适合看什么**：局部裁图、纹理切片

### 2.23 `dfs`
- **启动方式**：运行 DFS 演示入口
- **依赖**：无
- **注意**：递归深度大时要防止栈过深
- **适合看什么**：深度优先搜索、网格遍历

### 2.24 `down`
- **启动方式**：运行下载测试入口
- **依赖**：网络访问、HTTP
- **注意**：网络环境不通时会失败
- **适合看什么**：文件下载、进度回调

### 2.25 `dyn`
- **启动方式**：运行动态效果入口
- **依赖**：Scene2D
- **注意**：看对象是否在每帧正确刷新
- **适合看什么**：实时生成/销毁、动态状态

### 2.26 `ecode`
- **启动方式**：运行二维码入口
- **依赖**：二维码库 / 图片生成
- **注意**：输出图片尺寸和容错率要设置合理
- **适合看什么**：二维码生成与识别

### 2.27 `effect`
- **启动方式**：运行视觉特效入口
- **依赖**：Shader / 粒子 / 动画
- **注意**：效果模块通常需要素材支持
- **适合看什么**：爆炸、闪光、视觉组合效果

### 2.28 `endless`
- **启动方式**：运行无限滚动入口
- **依赖**：无
- **注意**：背景复位点要设置正确
- **适合看什么**：无限背景、循环移动

### 2.29 `event`
- **启动方式**：运行事件系统入口
- **依赖**：事件总线 / 监听机制
- **注意**：注意订阅和注销时机
- **适合看什么**：模块解耦、消息传递

### 2.30 `file`
- **启动方式**：运行 `TestFile.java` / `FileConvert.java`
- **依赖**：文件 IO、XML/PLIST
- **注意**：读写文件注意平台路径差异
- **适合看什么**：文本写入、坐标转换、资源处理

### 2.31 `fivestar`
- **启动方式**：运行星级评分入口
- **依赖**：触摸 / UI
- **注意**：星星点击区域要准确
- **适合看什么**：评分控件

### 2.32 `format`
- **启动方式**：运行格式化测试入口
- **依赖**：字符串处理
- **注意**：单位转换要统一
- **适合看什么**：数字格式化、文本格式

### 2.33 `freecenterscale`
- **启动方式**：运行缩放拖拽入口
- **依赖**：手势识别
- **注意**：缩放中心计算是核心
- **适合看什么**：地图缩放、图片自由缩放

### 2.34 `game`
- **启动方式**：运行 `GameTest.java`
- **依赖**：综合
- **注意**：通常是整合测试
- **适合看什么**：整体逻辑验证

### 2.35 `generator`
- **启动方式**：运行代码生成入口
- **依赖**：模板 / 文件输出
- **注意**：生成目录权限要正确
- **适合看什么**：自动化生成代码

### 2.36 `hit`
- **启动方式**：运行命中检测入口
- **依赖**：Actor / 几何计算
- **注意**：坐标转换要正确
- **适合看什么**：命中判断、碰撞区域

### 2.37 `interf`
- **启动方式**：运行接口示例入口
- **依赖**：无
- **注意**：重点看接口抽象方式
- **适合看什么**：接口设计、回调模式

### 2.38 `json`
- **启动方式**：运行 JSON 转 JavaBean 入口
- **依赖**：JSON 解析
- **注意**：复杂嵌套对象要留意命名冲突
- **适合看什么**：JSON 解析、代码生成

### 2.39 `label`
- **启动方式**：运行文本控件入口
- **依赖**：BitmapFont / Label
- **注意**：字体资源缺失会导致显示异常
- **适合看什么**：文字显示、样式控制

### 2.40 `language`
- **启动方式**：运行国际化入口
- **依赖**：I18NBundle
- **注意**：语言包路径要匹配
- **适合看什么**：多语言切换

### 2.41 `learn`
- **启动方式**：运行学习示例入口
- **依赖**：示例资源
- **注意**：偏教学性质
- **适合看什么**：入门型完整案例

### 2.42 `lib3d`
- **启动方式**：运行 3D 入门入口
- **依赖**：3D 模型 / 场景
- **注意**：确认 3D 资源完整
- **适合看什么**：3D 视角和模型基础

### 2.43 `light`
- **启动方式**：运行光照入口
- **依赖**：Shader / 光照逻辑
- **注意**：对 OpenGL 和渲染管线较敏感
- **适合看什么**：点光源、阴影、照明效果

### 2.44 `line`
- **启动方式**：运行 `LineTime.java`
- **依赖**：TextureWrap / Action
- **注意**：纹理重复模式要开启
- **适合看什么**：线条增长动画

### 2.45 `listener`
- **启动方式**：运行监听器入口
- **依赖**：事件监听
- **注意**：看回调触发顺序
- **适合看什么**：事件监听基础

### 2.46 `lizi`
- **启动方式**：运行粒子系统入口
- **依赖**：ParticleEffect / 粒子资源
- **注意**：粒子文件和纹理要完整
- **适合看什么**：粒子按钮、粒子进度条

### 2.47 `log`
- **启动方式**：运行日志入口
- **依赖**：无
- **注意**：输出级别和文件写入逻辑
- **适合看什么**：调试日志、日志封装

### 2.48 `mdesl`
- **启动方式**：运行固定列表相关入口
- **依赖**：Array<T>
- **注意**：适合复用，不只是演示
- **适合看什么**：固定容量队列、拖尾数据

### 2.49 `mesh`
- **启动方式**：运行 Mesh 示例入口
- **依赖**：Mesh
- **注意**：顶点和索引数组要正确
- **适合看什么**：底层几何绘制

### 2.50 `model`
- **启动方式**：运行模型示例入口
- **依赖**：3D 模型资源
- **注意**：透明模型和 decal 可能需要额外资源
- **适合看什么**：模型加载、透明渲染

### 2.51 `modelnew`
- **启动方式**：运行新版模型入口
- **依赖**：3D 模型资源
- **注意**：看新旧结构差异
- **适合看什么**：3D 新封装

### 2.52 `moni`
- **启动方式**：运行模拟入口
- **依赖**：无
- **注意**：实验性质强
- **适合看什么**：功能验证

### 2.53 `movetest`
- **启动方式**：运行移动测试入口
- **依赖**：无
- **注意**：关注速度、边界和插值
- **适合看什么**：对象移动控制

### 2.54 `mult`
- **启动方式**：运行多对象入口
- **依赖**：无
- **注意**：多对象场景注意性能
- **适合看什么**：对象批量管理

### 2.55 `net`
- **启动方式**：运行 `NetTest.java`
- **依赖**：网络 / HTTP
- **注意**：网络请求可能阻塞或超时
- **适合看什么**：HTTP 请求与回调

### 2.56 `npath`
- **启动方式**：运行寻路入口
- **依赖**：网格 / 路径算法
- **注意**：障碍物与权重设置
- **适合看什么**：路径搜索、导航

### 2.57 `other`
- **启动方式**：运行 Shader 变形入口
- **依赖**：ShaderProgram
- **注意**：外部 shader 文件路径要正确
- **适合看什么**：完整 GLSL 加载流程

### 2.58 `pan`
- **启动方式**：运行拖拽入口
- **依赖**：触摸 / 输入
- **注意**：边界限制和速度控制
- **适合看什么**：平移交互

### 2.59 `path`
- **启动方式**：运行路径跟随入口
- **依赖**：路径点
- **注意**：采样点和插值要平滑
- **适合看什么**：路径动画

### 2.60 `pengzhuang`
- **启动方式**：运行 `BroadApp.java`
- **依赖**：碰撞检测逻辑
- **注意**：宽相位和精确检测可能分两步
- **适合看什么**：AABB / 命中判断

### 2.61 `pet`
- **启动方式**：运行宠物跟随入口
- **依赖**：状态机 / 行为逻辑
- **注意**：跟随目标和距离阈值
- **适合看什么**：AI 跟随

### 2.62 `pic`
- **启动方式**：运行图片处理入口
- **依赖**：Texture / Pixmap
- **注意**：图片格式支持要确认
- **适合看什么**：图像变换

### 2.63 `pictureTrail`
- **启动方式**：运行拖尾入口
- **依赖**：轨迹数据
- **注意**：缓存点数量和透明度衰减
- **适合看什么**：尾迹、残影

### 2.64 `pix`
- **启动方式**：运行像素操作入口
- **依赖**：Pixmap
- **注意**：像素遍历量大时要注意性能
- **适合看什么**：像素读写、滤镜

### 2.65 `pixmap`
- **启动方式**：运行纹理生成入口
- **依赖**：Pixmap
- **注意**：生成结果要及时转 Texture
- **适合看什么**：程序化绘制

### 2.66 `point`
- **启动方式**：运行点坐标入口
- **依赖**：无
- **注意**：坐标原点要明确
- **适合看什么**：点运算

### 2.67 `poly`
- **启动方式**：运行 `PolyActor.java` 或 `WorldPolygonTest.java`
- **依赖**：PolygonSprite / Triangulator / ShapeRenderer
- **注意**：多边形点顺序和闭合性要正确
- **适合看什么**：任意多边形渲染

### 2.68 `position`
- **启动方式**：运行定位入口
- **依赖**：无
- **注意**：看坐标系转换
- **适合看什么**：锚点、位置调整

### 2.69 `process`
- **启动方式**：运行进度条入口
- **依赖**：Scene2D
- **注意**：进度值范围要固定
- **适合看什么**：线性进度

### 2.70 `qx`
- **启动方式**：运行曲线绘制入口
- **依赖**：数学计算
- **注意**：采样与可视化
- **适合看什么**：曲线绘制

### 2.71 `ray`
- **启动方式**：运行射线检测入口
- **依赖**：Intersector / 射线计算
- **注意**：从相机到世界坐标的转换
- **适合看什么**：Raycast、拾取

### 2.72 `render`
- **启动方式**：运行渲染管线入口
- **依赖**：FrameBuffer / OpenGL
- **注意**：注意 FBO 尺寸和切换顺序
- **适合看什么**：离屏渲染、后处理

### 2.73 `roll`
- **启动方式**：运行滚动效果入口
- **依赖**：无
- **注意**：滚动位移和复位点
- **适合看什么**：滚动动画

### 2.74 `sc`
- **启动方式**：运行 `sc/App.java`
- **依赖**：ScrollPane / Scene2D / FixedList
- **注意**：这是虚拟列表，重点看复用逻辑
- **适合看什么**：性能友好的长列表

### 2.75 `scissortest`
- **启动方式**：运行剪切测试入口
- **依赖**：Scissor / OpenGL
- **注意**：裁剪区设置不对会看不到内容
- **适合看什么**：剪切区域验证

### 2.76 `screen`
- **启动方式**：运行屏幕切换入口
- **依赖**：Screen 接口
- **注意**：切换时记得释放前一屏资源
- **适合看什么**：多屏管理

### 2.77 `scrollpanel`
- **启动方式**：运行滚动面板入口
- **依赖**：ScrollPane
- **注意**：内容高度要大于视口
- **适合看什么**：滚动 UI

### 2.78 `scrollroll`
- **启动方式**：运行双向滚动入口
- **依赖**：ScrollPane / 输入
- **注意**：X/Y 方向滚动都要调通
- **适合看什么**：双轴滚动

### 2.79 `shader`
- **启动方式**：运行 `HuiDuZhuanC.java` 或其他 Shader 示例
- **依赖**：GLSL / ShaderProgram / shader 资源
- **注意**：顶点和片元文件路径必须正确
- **适合看什么**：灰度、波浪、星空、水波等效果

### 2.80 `shaper`
- **启动方式**：运行 ShapeRenderer 入口
- **依赖**：ShapeRenderer
- **注意**：仅适合基础几何绘制
- **适合看什么**：线框、矩形、圆形

### 2.81 `sixteen`
- **启动方式**：运行十六进制入口
- **依赖**：字符串 / 位运算
- **注意**：输入格式要统一
- **适合看什么**：编码转换、二进制处理

### 2.82 `spine`
- **启动方式**：运行 `SpineTest.java`
- **依赖**：Spine runtime / spine 资源
- **注意**：骨骼资源、atlas、json 要完整
- **适合看什么**：骨骼动画播放

### 2.83 `spineanimation`
- **启动方式**：运行 Spine 切换控制入口
- **依赖**：Spine runtime
- **注意**：动画混合和状态切换是重点
- **适合看什么**：动作切换、过渡控制

### 2.84 `sprite`
- **启动方式**：运行精灵渲染入口
- **依赖**：SpriteBatch
- **注意**：Texture 资源要存在
- **适合看什么**：Sprite 的基本变换

### 2.85 `stencil`
- **启动方式**：运行 `Test.java` / `Cir.java` / `SeneTest.java`
- **依赖**：Stencil Buffer
- **注意**：显卡/OpenGL 版本兼容性很重要
- **适合看什么**：模板缓冲、遮罩

### 2.86 `table`
- **启动方式**：运行 Table 布局入口
- **依赖**：Scene2D UI
- **注意**：Table 的 grow / fill / pad 很关键
- **适合看什么**：UI 排版

### 2.87 `task`
- **启动方式**：运行异步任务入口
- **依赖**：线程 / 回调
- **注意**：主线程和后台线程的边界
- **适合看什么**：异步任务处理

### 2.88 `terrin`
- **启动方式**：运行 `TerrinApp.java`
- **依赖**：Pixmap / 地形生成逻辑
- **注意**：高度图尺寸较大，注意性能
- **适合看什么**：heightmap 生成

### 2.89 `tetris`
- **启动方式**：运行 `TetrisApp.java`
- **依赖**：无
- **注意**：方块旋转和消行是重点
- **适合看什么**：完整小游戏

### 2.90 `textfield`
- **启动方式**：运行文本输入入口
- **依赖**：Scene2D UI
- **注意**：输入法和焦点管理
- **适合看什么**：文本框交互

### 2.91 `thread`
- **启动方式**：运行 `ThreadTest.java`
- **依赖**：线程 / 回调 / 资源加载
- **注意**：后台线程不要直接操作 GL 对象
- **适合看什么**：异步加载、UI 回调

### 2.92 `throwa`
- **启动方式**：运行抛物线入口
- **依赖**：数学计算
- **注意**：重力和初速度要合理
- **适合看什么**：抛射轨迹

### 2.93 `tietu`
- **启动方式**：运行 UV 操作入口
- **依赖**：纹理坐标
- **注意**：UV 翻转和偏移
- **适合看什么**：贴图映射

### 2.94 `time`
- **启动方式**：运行时间倒计时入口
- **依赖**：Timer / Date / 服务器时间解析
- **注意**：GMT 解析格式要正确
- **适合看什么**：倒计时、时间格式化

### 2.95 `touch`
- **启动方式**：运行多点触控入口
- **依赖**：输入系统
- **注意**：多指 ID 追踪
- **适合看什么**：双指缩放、旋转

### 2.96 `trile`
- **启动方式**：运行 Tiled 地图入口
- **依赖**：TiledMap / 地图资源
- **注意**：tmx / 图片 / tileset 必须完整
- **适合看什么**：地图加载、对象层

### 2.97 `trycatch`
- **启动方式**：运行异常处理入口
- **依赖**：无
- **注意**：看异常捕获和日志输出
- **适合看什么**：容错处理

### 2.98 `tt`
- **启动方式**：运行综合测试入口
- **依赖**：不固定
- **注意**：临时测试、组合验证
- **适合看什么**：杂项整合

### 2.99 `vect`
- **启动方式**：运行向量运算入口
- **依赖**：数学 / Vector 类
- **注意**：角度、长度、单位向量
- **适合看什么**：向量基础运算

### 2.100 `version`
- **启动方式**：运行版本管理入口
- **依赖**：无
- **注意**：版本字符串匹配
- **适合看什么**：版本判断、升级逻辑

### 2.101 `vertices`
- **启动方式**：运行顶点操作入口
- **依赖**：Mesh / 顶点数据
- **注意**：顶点布局顺序必须对
- **适合看什么**：底层顶点处理

### 2.102 `video`
- **启动方式**：运行视频播放入口
- **依赖**：视频资源 / 播放器
- **注意**：编码格式和平台支持
- **适合看什么**：视频播放

### 2.103 `view`
- **启动方式**：运行视口管理入口
- **依赖**：Viewport
- **注意**：分辨率适配参数
- **适合看什么**：适配 / 视口

### 2.104 `wak`
- **启动方式**：运行行走/导航入口
- **依赖**：路径 / 运动逻辑
- **注意**：移动状态和路径点
- **适合看什么**：导航与行走动画

### 2.105 `wakong`
- **启动方式**：运行 `wakong/App.java`
- **依赖**：遮罩 / 裁切
- **注意**：挖空区域和黑幕层级
- **适合看什么**：引导遮罩、聚光灯效果

### 2.106 `watch`
- **启动方式**：运行 `WatchDogTest.java`
- **依赖**：时间 / 动画 / UI
- **注意**：看时钟动画和监控逻辑
- **适合看什么**：表盘、时钟、看门狗

### 2.107 `xiaoguo`
- **启动方式**：运行特效综合入口
- **依赖**：Shader / 粒子 / 动画
- **注意**：通常会依赖较多资源
- **适合看什么**：综合视觉效果

### 2.108 `xml`
- **启动方式**：运行 XML 解析入口
- **依赖**：XML 文件
- **注意**：节点路径和属性名要对
- **适合看什么**：XML 配置读取

### 2.109 `zhujie`
- **启动方式**：运行注解示例入口
- **依赖**：Java Annotation / 反射
- **注意**：注意注解保留策略
- **适合看什么**：注解、反射、元数据

---

## 3. 如何和主文档配合使用

建议主文档保留为：

- 仓库总览
- 分类索引
- 重点案例解析
- 标签表

而这份运行说明文档保留为：

- 启动方式
- 依赖
- 注意事项
- 适合看什么

这样两个文档分工明确：

- `DESKTOP_CASES_DOC.md`：看“是什么、为什么重要”
- `DESKTOP_CASES_RUNBOOK.md`：看“怎么运行、怎么读”

---

## 4. 建议下一步

如果你愿意，我下一步可以继续帮你做两件事中的任意一个：

1. **把这份运行说明再压缩成更适合 GitHub README 阅读的简版**
2. **把这份运行说明真正合并进仓库的 `README.md` / 主文档索引结构里**

